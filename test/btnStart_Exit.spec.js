describe('Test btnStart.js', () => {
  it('Btn start should be equal undefined', () => {
    expect(buttonStart()).to.equal(undefined);
  });
  it('Btn exit should be equal undefined', () => {
    expect(buttonExit()).to.equal(undefined);
  });
});
